---
description: 
globs: 
alwaysApply: true
---
# Styling components

When styling .svelte components, always use the global app.css file. Never use svelte's built-in <style> tag.

# Classes

When possible, reuse the existing classes from app.css instead of creating new ones.

# Units

Never use rem or em units. Stick to px and % units.
